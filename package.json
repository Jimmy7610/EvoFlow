{
  "name": "evoflow-root-runner",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "fix:workspace": "powershell -ExecutionPolicy Bypass -File \".\\tools\\fix-workspace-deps.ps1\"",
    "install:web": "npm --prefix apps/web install",
    "install:api": "npm --prefix apps/api install",
    "install:worker": "npm --prefix workers/executor install",
    "install:all": "npm run install:web && npm run install:api && npm run install:worker",
    "dev": "concurrently -n \"WEB,API,WRK\" -c \"cyan,magenta,yellow\" --kill-others \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix workers/executor\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
